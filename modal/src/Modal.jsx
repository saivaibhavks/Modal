import { useState } from "react";
import "./Modal.css";

const Modal = () => {
  cosnt[(shoModal, setShowModal)] = useState(false);
  return (
    <div className="container">
      <button>Click me</button>
    </div>
  );
};

export default Modal;
